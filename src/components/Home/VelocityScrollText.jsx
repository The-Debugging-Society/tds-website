import { VelocityScroll } from "../ui/scroll-based-velocity";

export function VelocityScrollText() {
  return <VelocityScroll className={"bg-black p-4 text-blue-400"} numRows={2}>
      The Debugging Society(TDS)
    </VelocityScroll>;
}
